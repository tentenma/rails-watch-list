<h1>Lists#show</h1>

<div class="container py-5">

  <h1><%= @list.name %></h1>
  <p>Description</p>

  <span><%= link_to "Return home", lists_index_path,  class: "btn btn-warning d-inline p-2" %></span>

  <%# < class="d-block p-2 text-bg-dark">d-block</span> %>
    <span> <%= link_to "Remove", list_path(@list),
      class: "btn btn-secondary d-inline p-2", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this list?" }
      %>
    </span>
    <!-- Content here -->
</div>
